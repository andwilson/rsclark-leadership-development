import Typography from "typography";

const typography = new Typography({
  baseFontSize: "14px",
  baseLineHeight: 1.45,
  headerFontFamily: [
    "Roboto",
    "sans-serif",
  ],
  bodyFontFamily: [
    "Open Sans",
    "sans-serif"
  ],
});

export default typography;
