import Typography from "typography";

const typography = new Typography({
  baseFontSize: "12px",
  baseLineHeight: 1.45,
  headerFontFamily: [
    "Cutive",
    "serif",
  ],
  bodyFontFamily: [
    "Open Sans",
    "sans-serif"
  ],
});

export default typography;
